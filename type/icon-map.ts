// icon-map.ts (client-safe)
import {
  AudioWaveform,
  BookOpen,
  Bot,
  Command,
  Frame,
  GalleryVerticalEnd,
  Map,
  PieChart,
  Settings2,
  SquareTerminal,
} from "lucide-react";

export const iconMap = {
  gallery: GalleryVerticalEnd,
  audio: AudioWaveform,
  command: Command,
  terminal: SquareTerminal,
  bot: Bot,
  book: BookOpen,
  settings: Settings2,
  frame: Frame,
  pie: PieChart,
  map: Map,
};
